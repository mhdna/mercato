// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: entry.sql

package db

import (
	"context"
)

const createEntryItem = `-- name: CreateEntryItem :one
INSERT INTO entries (
  cashbox_id,
  inventory_id,
  reference_type,
  reference_id,
  amount
) 
VALUES ( $1, $2, $3, $4, $5)
RETURNING id, cashbox_id, inventory_id, reference_type, reference_id, amount, created_at
`

type CreateEntryItemParams struct {
	CashboxID     int64              `json:"cashbox_id"`
	InventoryID   int64              `json:"inventory_id"`
	ReferenceType EntryReferenceType `json:"reference_type"`
	ReferenceID   int64              `json:"reference_id"`
	Amount        int64              `json:"amount"`
}

func (q *Queries) CreateEntryItem(ctx context.Context, arg CreateEntryItemParams) (Entry, error) {
	row := q.db.QueryRowContext(ctx, createEntryItem,
		arg.CashboxID,
		arg.InventoryID,
		arg.ReferenceType,
		arg.ReferenceID,
		arg.Amount,
	)
	var i Entry
	err := row.Scan(
		&i.ID,
		&i.CashboxID,
		&i.InventoryID,
		&i.ReferenceType,
		&i.ReferenceID,
		&i.Amount,
		&i.CreatedAt,
	)
	return i, err
}

const getEntry = `-- name: GetEntry :one
SELECT id, cashbox_id, inventory_id, reference_type, reference_id, amount, created_at FROM entries
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetEntry(ctx context.Context, id int64) (Entry, error) {
	row := q.db.QueryRowContext(ctx, getEntry, id)
	var i Entry
	err := row.Scan(
		&i.ID,
		&i.CashboxID,
		&i.InventoryID,
		&i.ReferenceType,
		&i.ReferenceID,
		&i.Amount,
		&i.CreatedAt,
	)
	return i, err
}

const listEntries = `-- name: ListEntries :many
SELECT id, cashbox_id, inventory_id, reference_type, reference_id, amount, created_at FROM entries
WHERE inventory_id = $1
ORDER BY id
LIMIT $2
OFFSET $3
`

type ListEntriesParams struct {
	InventoryID int64 `json:"inventory_id"`
	Limit       int32 `json:"limit"`
	Offset      int32 `json:"offset"`
}

func (q *Queries) ListEntries(ctx context.Context, arg ListEntriesParams) ([]Entry, error) {
	rows, err := q.db.QueryContext(ctx, listEntries, arg.InventoryID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Entry{}
	for rows.Next() {
		var i Entry
		if err := rows.Scan(
			&i.ID,
			&i.CashboxID,
			&i.InventoryID,
			&i.ReferenceType,
			&i.ReferenceID,
			&i.Amount,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
