// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: invoice_payment.sql

package db

import (
	"context"
)

const createInvoicePayment = `-- name: CreateInvoicePayment :one
INSERT INTO invoice_payments (invoice_id, cashbox_account_id, amount)
VALUES ($1, $2, $3)
RETURNING id, invoice_id, cashbox_account_id, amount
`

type CreateInvoicePaymentParams struct {
	InvoiceID        int64 `json:"invoice_id"`
	CashboxAccountID int64 `json:"cashbox_account_id"`
	Amount           int64 `json:"amount"`
}

func (q *Queries) CreateInvoicePayment(ctx context.Context, arg CreateInvoicePaymentParams) (InvoicePayment, error) {
	row := q.db.QueryRowContext(ctx, createInvoicePayment, arg.InvoiceID, arg.CashboxAccountID, arg.Amount)
	var i InvoicePayment
	err := row.Scan(
		&i.ID,
		&i.InvoiceID,
		&i.CashboxAccountID,
		&i.Amount,
	)
	return i, err
}

const listInvoicePaymentsByInvoice = `-- name: ListInvoicePaymentsByInvoice :many
SELECT invoice_payments.id, invoice_payments.invoice_id, invoice_payments.cashbox_account_id, invoice_payments.amount, cashbox_accounts.name AS account_name
FROM invoice_payments
JOIN cashbox_accounts ON cashbox_accounts.id = invoice_payments.cashbox_account_id
WHERE invoice_payments.invoice_id = $1
ORDER BY invoice_payments.id
`

type ListInvoicePaymentsByInvoiceRow struct {
	ID               int64  `json:"id"`
	InvoiceID        int64  `json:"invoice_id"`
	CashboxAccountID int64  `json:"cashbox_account_id"`
	Amount           int64  `json:"amount"`
	AccountName      string `json:"account_name"`
}

func (q *Queries) ListInvoicePaymentsByInvoice(ctx context.Context, invoiceID int64) ([]ListInvoicePaymentsByInvoiceRow, error) {
	rows, err := q.db.QueryContext(ctx, listInvoicePaymentsByInvoice, invoiceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListInvoicePaymentsByInvoiceRow{}
	for rows.Next() {
		var i ListInvoicePaymentsByInvoiceRow
		if err := rows.Scan(
			&i.ID,
			&i.InvoiceID,
			&i.CashboxAccountID,
			&i.Amount,
			&i.AccountName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
