# Project instructions

- This project uses a custom MDI icon set in `src/plugins/icons.js`. Whenever a new `mdi-*` icon is referenced, add its matching import from `@mdi/js` and register it in `iconPaths`; do not rely on Vuetify's default icon set.
